1- GO to the back end folder
2- type command 'npm install' // it will download all the required modules
3- type command 'node index'  // it will run the server at localhost:8000
4- Go frontend Flder
5- type command 'npm install' // it will download all the required modules
6- type command 'ng serve -o' // it will serve the provject at localhost:4200



** make sure both the port are free to use

